{
  "identifier": "default",
  "description": "Default capability for the DNS Wizard app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-open",
    "dialog:default"
  ]
}